<div id="burger-menu-wrapper">
    <button id="burger-toggle" aria-label="Menü öffnen">
        <span class="burger-line"></span>
        <span class="burger-line"></span>
        <span class="burger-line"></span>
    </button>

    <nav id="burger-menu">
        <a href="{{ languageMap['concept-art'].de }}" class="{% if (page.url or '').startsWith(languageMap['concept-art'].de) %}active{% endif %}">concept art</a>
        <a href="{{ languageMap['games'].de }}" class="{% if (page.url or '').startsWith(languageMap['games'].de) %}active{% endif %}">games</a>
        <a href="{{ languageMap['kunst'].de }}" class="{% if (page.url or '').startsWith(languageMap['kunst'].de) %}active{% endif %}">kunst</a>
        <a href="{{ languageMap['ueber'].de }}" class="{% if (page.url or '').startsWith(languageMap['ueber'].de) %}active{% endif %}">über mich</a>
    </nav>

    <div id="burger-overlay"></div>
</div>