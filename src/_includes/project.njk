<!DOCTYPE html>
<html lang="{{ lang }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

  <header>
    <a href="{% if lang == 'de' %}/index.html{% else %}/en/index.html{% endif %}" class="home">INSOMNEA art</a>
  </header>

  <main>
    {# Hero Section #}
    <section class="hero">
      <img src="{{ heroImage }}" alt="{{ heroAlt }}" class="hero-image">
      <div class="hero-text">
        {{ heroText | safe }}
      </div>
    </section>

    <hr>

    {# Hier kommt der manuell eingefüllte Inhalt der Seite #}
    {{ content | safe }}

  </main>

  <footer>

  </footer>

</body>
</html>
