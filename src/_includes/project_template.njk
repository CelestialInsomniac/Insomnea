<main>
  <h1 class="project-title">{{ title }}</h1>

  {# Hero Section #}
  <section class="hero">
    <img src="{{ heroImage }}" alt="{{ heroAlt }}" class="hero-image">
    <div class="hero-text">
      {{ heroText | safe }}
    </div>
  </section>

  <hr>

  {{ content | safe }}
</main>